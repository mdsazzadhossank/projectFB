# Dependencies
node_modules
api/vendor

# Build output
dist

# Packaging / artifacts
messagehub-deploy.zip
api/api.zip

# Claude session/tooling files
.claude/

# Deployed/local backend config — never commit real credentials
api/config.local.php

# Logs
*.log
npm-debug.log*

# Environment / secrets — never commit real credentials
.env
.env.*
!.env.example

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/
